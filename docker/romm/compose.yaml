services:
  romm:
    image: rommapp/romm:latest
    container_name: romm
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_HOST=romm-db
      - DB_NAME=romm
      - DB_USER=romm-user
      - DB_PASSWD=${MARIADB_PASSWORD}
    volumes:
      - /mnt/main/appdata/romm/assets:/romm/assets
      - /mnt/main/appdata/romm/config:/romm/config
      - /mnt/main/appdata/romm/romm_redis_data:/redis-data
      - /mnt/main/appdata/romm/romm_resources:/romm/resources
      - /mnt/remotes/unas-pro/unraid/romm-library:/romm/library
    ports:
      - 8280:8080
    depends_on:
      romm-db:
        condition: service_healthy
        restart: true

  romm-db:
    image: mariadb:latest
    container_name: romm-db
    restart: unless-stopped
    env_file: .env
    environment:
      - MARIADB_DATABASE=romm
      - MARIADB_USER=romm-user
    volumes:
      - /mnt/main/appdata/romm/mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      start_period: 30s
      start_interval: 10s
      interval: 10s
      timeout: 5s
      retries: 5
