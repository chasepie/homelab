services:
  web:
    image: ghcr.io/karakeep-app/karakeep:${KARAKEEP_VERSION:-release}
    restart: unless-stopped
    volumes:
      - /mnt/main/appdata/karakeep/data:/data
    ports:
      - 3000:3000
    env_file: .env
    environment:
      MEILI_ADDR: http://meilisearch:7700
      BROWSER_WEB_URL: http://chrome:9222
      OPENAI_BASE_URL: http://host.docker.internal:3100/api
      INFERENCE_TEXT_MODEL: "gemma3:latest"
      INFERENCE_IMAGE_MODEL: "llava:latest"
      INFERENCE_CONTEXT_LENGTH: 4096
      INFERENCE_ENABLE_AUTO_TAGGING: true
      INFERENCE_ENABLE_AUTO_SUMMARIZATION: true
      OCR_USE_LLM: true
      DATA_DIR: /data # DON'T CHANGE THIS
      DISABLE_SIGNUPS: true
    extra_hosts:
      - "host.docker.internal:host-gateway"

  chrome:
    image: gcr.io/zenika-hub/alpine-chrome:124
    restart: unless-stopped
    command:
      - --no-sandbox
      - --disable-gpu
      - --disable-dev-shm-usage
      - --remote-debugging-address=0.0.0.0
      - --remote-debugging-port=9222
      - --hide-scrollbars

  meilisearch:
    image: getmeili/meilisearch:v1.13.3
    restart: unless-stopped
    env_file: .env
    environment:
      MEILI_NO_ANALYTICS: "true"
    volumes:
      - /mnt/main/appdata/karakeep/meilisearch:/meili_data
